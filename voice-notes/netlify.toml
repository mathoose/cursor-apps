# Legacy package directory for the voice-notes-mattheus Netlify site.
# Stage the monorepo root into dist/ because Netlify won't publish outside
# the package directory.
[build]
  command = "bash build-netlify.sh"
  publish = "dist"
