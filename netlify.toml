# Shared security headers (build settings live in voice-notes/netlify.toml for
# the legacy voice-notes-mattheus Netlify site's package directory).

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
